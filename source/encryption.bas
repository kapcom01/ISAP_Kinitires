Attribute VB_Name = "encryption"
Public Function encrypt(text As String)
Dim p As Integer
Dim temp As String

    For p = 1 To Len(text)
        If Mid(text, p, 1) = a Then
            temp = temp & m
        End If
    Next
End Function
